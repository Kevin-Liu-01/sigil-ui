node_modules/
dist/
.turbo/
.next/
.source/
*.tsbuildinfo
.env
.env.local

# Generated token preview (compiled via apps/web/scripts/build-tokens-css.mjs)
apps/web/app/_generated/*.preview.css
.playwright-cli/
.playwright-mcp/
output/playwright/
# Timestamped audit runs are large and ephemeral. The committed report
# (AUDIT_REPORT.md) plus the latest run live alongside this file.
output/audit/2*/
output/audit/*.log

# Ad-hoc PNG screenshots dumped at the repo root by visual-debugging sessions
# (drift checks, alignment overlays, etc). Any images we want to keep belong
# in apps/*/public/ or docs/, never at the repo root.
/*.png
