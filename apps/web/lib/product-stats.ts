export const SIGIL_PRODUCT_STATS = {
  componentCountLabel: "200+",
  presetCount: 46,
  tokenCount: 519,
} as const;

export const SIGIL_PRODUCT_SUMMARY = `${SIGIL_PRODUCT_STATS.componentCountLabel} token-driven components, ${SIGIL_PRODUCT_STATS.tokenCount} tokens, ${SIGIL_PRODUCT_STATS.presetCount} presets`;
