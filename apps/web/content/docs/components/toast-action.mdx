---
title: ToastAction
description: ToastAction is a token-driven Sigil component in the feedback and status group.
---

## Import

```tsx
import { ToastAction } from "@sigil-ui/components";
```

## Preview

<ComponentPreview>
  <ToastAction>Undo</ToastAction>
</ComponentPreview>

## Usage

```tsx
<ToastAction>Undo</ToastAction>
```

## CLI

```bash
npx @sigil-ui/cli add toast-action
```

## Token Integration

ToastAction follows the Sigil component rules: styles resolve from `var(--s-*)`, class names are mergeable through `className`, and the component is exported from `@sigil-ui/components`.
