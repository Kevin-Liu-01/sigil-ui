---
title: SwitchField
description: SwitchField is a token-driven Sigil component in the forms and inputs group.
---

## Import

```tsx
import { SwitchField } from "@sigil-ui/components";
```

## Preview

<ComponentPreview>
  <SwitchField label="Notifications" description="Receive product updates." defaultChecked />
</ComponentPreview>

## Usage

```tsx
<SwitchField label="Notifications" description="Receive product updates." defaultChecked />
```

## CLI

```bash
npx @sigil-ui/cli add switch-field
```

## Token Integration

SwitchField follows the Sigil component rules: styles resolve from `var(--s-*)`, class names are mergeable through `className`, and the component is exported from `@sigil-ui/components`.
