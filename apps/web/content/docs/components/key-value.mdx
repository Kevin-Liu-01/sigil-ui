---
title: KeyValue
description: KeyValue is a token-driven Sigil component in the data display and tables group.
---

## Import

```tsx
import { KeyValue } from "@sigil-ui/components";
```

## Preview

<ComponentPreview>
  <KeyValue label="Preset" value="sigil" />
</ComponentPreview>

## Usage

```tsx
<KeyValue label="Preset" value="sigil" />
```

## CLI

```bash
npx @sigil-ui/cli add key-value
```

## Token Integration

KeyValue follows the Sigil component rules: styles resolve from `var(--s-*)`, class names are mergeable through `className`, and the component is exported from `@sigil-ui/components`.
