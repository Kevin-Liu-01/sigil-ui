---
title: ErrorState
description: ErrorState is a token-driven Sigil component in the feedback and status group.
---

## Import

```tsx
import { ErrorState } from "@sigil-ui/components";
```

## Preview

<ComponentPreview>
  <ErrorState title="Something went wrong" description="Use an action to help users recover." />
</ComponentPreview>

## Usage

```tsx
<ErrorState title="Something went wrong" description="Use an action to help users recover." />
```

## CLI

```bash
npx @sigil-ui/cli add error-state
```

## Token Integration

ErrorState follows the Sigil component rules: styles resolve from `var(--s-*)`, class names are mergeable through `className`, and the component is exported from `@sigil-ui/components`.
