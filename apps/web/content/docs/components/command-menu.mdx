---
title: CommandMenu
description: CommandMenu is a token-driven Sigil component in the overlay and interaction group.
---

## Import

```tsx
import { CommandMenu } from "@sigil-ui/components";
```

## Preview

<ComponentPreview>
  <Modal trigger={<Button variant="outline">Open command-menu demo</Button>} title="CommandMenu" description="Use CommandMenu when the page owns an open state." />
</ComponentPreview>

## Usage

```tsx
<Modal trigger={<Button variant="outline">Open command-menu demo</Button>} title="CommandMenu" description="Use CommandMenu when the page owns an open state." />
```

## CLI

```bash
npx @sigil-ui/cli add command-menu
```

## Token Integration

CommandMenu follows the Sigil component rules: styles resolve from `var(--s-*)`, class names are mergeable through `className`, and the component is exported from `@sigil-ui/components`.
