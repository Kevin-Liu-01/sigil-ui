---
title: Callout
description: Callout is a token-driven Sigil component in the feedback and status group.
---

## Import

```tsx
import { Callout } from "@sigil-ui/components";
```

## Preview

<ComponentPreview>
  <Callout variant="info" title="Token-first" description="Recipes still read from CSS custom properties." />
</ComponentPreview>

## Usage

```tsx
<Callout variant="info" title="Token-first" description="Recipes still read from CSS custom properties." />
```

## CLI

```bash
npx @sigil-ui/cli add callout
```

## Token Integration

Callout follows the Sigil component rules: styles resolve from `var(--s-*)`, class names are mergeable through `className`, and the component is exported from `@sigil-ui/components`.
