---
title: FloatingPanel
description: FloatingPanel is a token-driven Sigil component in the overlay and interaction group.
---

## Import

```tsx
import { FloatingPanel } from "@sigil-ui/components";
```

## Preview

<ComponentPreview>
  <FloatingPanel>Floating panel content</FloatingPanel>
</ComponentPreview>

## Usage

```tsx
<FloatingPanel>Floating panel content</FloatingPanel>
```

## CLI

```bash
npx @sigil-ui/cli add floating-panel
```

## Token Integration

FloatingPanel follows the Sigil component rules: styles resolve from `var(--s-*)`, class names are mergeable through `className`, and the component is exported from `@sigil-ui/components`.
