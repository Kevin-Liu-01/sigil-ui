---
title: PromptDialog
description: PromptDialog is a token-driven Sigil component in the overlay and interaction group.
---

## Import

```tsx
import { PromptDialog } from "@sigil-ui/components";
```

## Preview

<ComponentPreview>
  <PromptDialog trigger={<Button variant="outline">Open prompt</Button>} title="Rename project" description="Collect a short value in a dialog." placeholder="Project name" />
</ComponentPreview>

## Usage

```tsx
<PromptDialog trigger={<Button variant="outline">Open prompt</Button>} title="Rename project" description="Collect a short value in a dialog." placeholder="Project name" />
```

## CLI

```bash
npx @sigil-ui/cli add prompt-dialog
```

## Token Integration

PromptDialog follows the Sigil component rules: styles resolve from `var(--s-*)`, class names are mergeable through `className`, and the component is exported from `@sigil-ui/components`.
