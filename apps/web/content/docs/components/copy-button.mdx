---
title: CopyButton
description: CopyButton is a token-driven Sigil component in the developer and ai/product ui group.
---

## Import

```tsx
import { CopyButton } from "@sigil-ui/components";
```

## Preview

<ComponentPreview>
  <CopyButton value="npx @sigil-ui/cli add button">Copy command</CopyButton>
</ComponentPreview>

## Usage

```tsx
<CopyButton value="npx @sigil-ui/cli add button">Copy command</CopyButton>
```

## CLI

```bash
npx @sigil-ui/cli add copy-button
```

## Token Integration

CopyButton follows the Sigil component rules: styles resolve from `var(--s-*)`, class names are mergeable through `className`, and the component is exported from `@sigil-ui/components`.
