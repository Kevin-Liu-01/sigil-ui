---
title: Modal
description: Modal is a token-driven Sigil component in the overlay and interaction group.
---

## Import

```tsx
import { Modal } from "@sigil-ui/components";
```

## Preview

<ComponentPreview>
  <Modal trigger={<Button>Open modal</Button>} title="Modal" description="Token-driven dialog shell." />
</ComponentPreview>

## Usage

```tsx
<Modal trigger={<Button>Open modal</Button>} title="Modal" description="Token-driven dialog shell." />
```

## CLI

```bash
npx @sigil-ui/cli add modal
```

## Token Integration

Modal follows the Sigil component rules: styles resolve from `var(--s-*)`, class names are mergeable through `className`, and the component is exported from `@sigil-ui/components`.
