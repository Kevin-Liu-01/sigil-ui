---
title: SearchInput
description: SearchInput is a token-driven Sigil component in the forms and inputs group.
---

## Import

```tsx
import { SearchInput } from "@sigil-ui/components";
```

## Preview

<ComponentPreview>
  <SearchInput placeholder="Search components..." />
</ComponentPreview>

## Usage

```tsx
<SearchInput placeholder="Search components..." />
```

## CLI

```bash
npx @sigil-ui/cli add search-input
```

## Token Integration

SearchInput follows the Sigil component rules: styles resolve from `var(--s-*)`, class names are mergeable through `className`, and the component is exported from `@sigil-ui/components`.
