---
title: PromptInput
description: PromptInput is a token-driven Sigil component in the developer and ai/product ui group.
---

## Import

```tsx
import { PromptInput } from "@sigil-ui/components";
```

## Preview

<ComponentPreview>
  <PromptInput placeholder="Ask Sigil to generate a component..." />
</ComponentPreview>

## Usage

```tsx
<PromptInput placeholder="Ask Sigil to generate a component..." />
```

## CLI

```bash
npx @sigil-ui/cli add prompt-input
```

## Token Integration

PromptInput follows the Sigil component rules: styles resolve from `var(--s-*)`, class names are mergeable through `className`, and the component is exported from `@sigil-ui/components`.
