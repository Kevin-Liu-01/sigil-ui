---
title: EmptyTable
description: EmptyTable is a token-driven Sigil component in the data display and tables group.
---

## Import

```tsx
import { EmptyTable } from "@sigil-ui/components";
```

## Preview

<ComponentPreview>
  <EmptyTable message="No components found." />
</ComponentPreview>

## Usage

```tsx
<EmptyTable message="No components found." />
```

## CLI

```bash
npx @sigil-ui/cli add empty-table
```

## Token Integration

EmptyTable follows the Sigil component rules: styles resolve from `var(--s-*)`, class names are mergeable through `className`, and the component is exported from `@sigil-ui/components`.
