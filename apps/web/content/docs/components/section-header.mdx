---
title: SectionHeader
description: SectionHeader is a token-driven Sigil component in the layout, navigation, and content group.
---

## Import

```tsx
import { SectionHeader } from "@sigil-ui/components";
```

## Preview

<ComponentPreview>
  <SectionHeader title="Section title" description="A section-level heading block." />
</ComponentPreview>

## Usage

```tsx
<SectionHeader title="Section title" description="A section-level heading block." />
```

## CLI

```bash
npx @sigil-ui/cli add section-header
```

## Token Integration

SectionHeader follows the Sigil component rules: styles resolve from `var(--s-*)`, class names are mergeable through `className`, and the component is exported from `@sigil-ui/components`.
