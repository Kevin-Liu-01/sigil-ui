---
title: Lightbox
description: Lightbox is a token-driven Sigil component in the overlay and interaction group.
---

## Import

```tsx
import { Lightbox } from "@sigil-ui/components";
```

## Preview

<ComponentPreview>
  <Lightbox trigger={<Button variant="outline">Open lightbox</Button>} src="https://placehold.co/640x360/png" alt="Placeholder image" caption="Image preview" />
</ComponentPreview>

## Usage

```tsx
<Lightbox trigger={<Button variant="outline">Open lightbox</Button>} src="https://placehold.co/640x360/png" alt="Placeholder image" caption="Image preview" />
```

## CLI

```bash
npx @sigil-ui/cli add lightbox
```

## Token Integration

Lightbox follows the Sigil component rules: styles resolve from `var(--s-*)`, class names are mergeable through `className`, and the component is exported from `@sigil-ui/components`.
