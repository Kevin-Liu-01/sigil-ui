---
title: ContainerQuery
description: ContainerQuery is a token-driven Sigil component in the layout, navigation, and content group.
---

## Import

```tsx
import { ContainerQuery } from "@sigil-ui/components";
```

## Preview

<ComponentPreview>
  <ContainerQuery><Card><CardHeader><CardTitle>Container-aware surface</CardTitle></CardHeader></Card></ContainerQuery>
</ComponentPreview>

## Usage

```tsx
<ContainerQuery><Card><CardHeader><CardTitle>Container-aware surface</CardTitle></CardHeader></Card></ContainerQuery>
```

## CLI

```bash
npx @sigil-ui/cli add container-query
```

## Token Integration

ContainerQuery follows the Sigil component rules: styles resolve from `var(--s-*)`, class names are mergeable through `className`, and the component is exported from `@sigil-ui/components`.
