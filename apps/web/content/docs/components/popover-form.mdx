---
title: PopoverForm
description: PopoverForm is a token-driven Sigil component in the overlay and interaction group.
---

## Import

```tsx
import { PopoverForm } from "@sigil-ui/components";
```

## Preview

<ComponentPreview>
  <PopoverForm trigger={<Button variant="outline">Open form</Button>}><Input placeholder="Email" /><Button>Submit</Button></PopoverForm>
</ComponentPreview>

## Usage

```tsx
<PopoverForm trigger={<Button variant="outline">Open form</Button>}><Input placeholder="Email" /><Button>Submit</Button></PopoverForm>
```

## CLI

```bash
npx @sigil-ui/cli add popover-form
```

## Token Integration

PopoverForm follows the Sigil component rules: styles resolve from `var(--s-*)`, class names are mergeable through `className`, and the component is exported from `@sigil-ui/components`.
