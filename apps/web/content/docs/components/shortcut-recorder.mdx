---
title: ShortcutRecorder
description: ShortcutRecorder is a token-driven Sigil component in the overlay and interaction group.
---

## Import

```tsx
import { ShortcutRecorder } from "@sigil-ui/components";
```

## Preview

<ComponentPreview>
  <ShortcutRecorder defaultValue={["Meta", "K"]} />
</ComponentPreview>

## Usage

```tsx
<ShortcutRecorder defaultValue={["Meta", "K"]} />
```

## CLI

```bash
npx @sigil-ui/cli add shortcut-recorder
```

## Token Integration

ShortcutRecorder follows the Sigil component rules: styles resolve from `var(--s-*)`, class names are mergeable through `className`, and the component is exported from `@sigil-ui/components`.
