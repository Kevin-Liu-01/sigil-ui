---
title: DateRangeField
description: DateRangeField is a token-driven Sigil component in the forms and inputs group.
---

## Import

```tsx
import { DateRangeField } from "@sigil-ui/components";
```

## Preview

<ComponentPreview>
  <DateRangeField label="Date range" placeholder="Pick a date range" />
</ComponentPreview>

## Usage

```tsx
<DateRangeField label="Date range" placeholder="Pick a date range" />
```

## CLI

```bash
npx @sigil-ui/cli add date-range-field
```

## Token Integration

DateRangeField follows the Sigil component rules: styles resolve from `var(--s-*)`, class names are mergeable through `className`, and the component is exported from `@sigil-ui/components`.
