---
title: ThemeSwatch
description: ThemeSwatch is a token-driven Sigil component in the developer and ai/product ui group.
---

## Import

```tsx
import { ThemeSwatch } from "@sigil-ui/components";
```

## Preview

<ComponentPreview>
  <ThemeSwatch name="Sigil" selected />
</ComponentPreview>

## Usage

```tsx
<ThemeSwatch name="Sigil" selected />
```

## CLI

```bash
npx @sigil-ui/cli add theme-swatch
```

## Token Integration

ThemeSwatch follows the Sigil component rules: styles resolve from `var(--s-*)`, class names are mergeable through `className`, and the component is exported from `@sigil-ui/components`.
