---
title: AvatarUpload
description: AvatarUpload is a token-driven Sigil component in the forms and inputs group.
---

## Import

```tsx
import { AvatarUpload } from "@sigil-ui/components";
```

## Preview

<ComponentPreview>
  <AvatarUpload previewUrl="https://placehold.co/640x360/png" label="Upload avatar" />
</ComponentPreview>

## Usage

```tsx
<AvatarUpload previewUrl="https://placehold.co/640x360/png" label="Upload avatar" />
```

## CLI

```bash
npx @sigil-ui/cli add avatar-upload
```

## Token Integration

AvatarUpload follows the Sigil component rules: styles resolve from `var(--s-*)`, class names are mergeable through `className`, and the component is exported from `@sigil-ui/components`.
