---
title: Dialog
description: "A Dialog component from the Sigil design system."
---

## Import

```tsx
import { Dialog, DialogTrigger, DialogClose, DialogPortal, DialogContent, DialogHeader, DialogTitle, DialogDescription, DialogFooter } from "@sigil-ui/components";
```

## Usage

```tsx
<Dialog>...</Dialog>
```

## Sub-components

This component includes the following parts:

- `<DialogTrigger>`
- `<DialogClose>`
- `<DialogPortal>`
- `<DialogContent>`
- `<DialogHeader>`
- `<DialogTitle>`
- `<DialogDescription>`
- `<DialogFooter>`

## Token integration

| CSS Variable | Purpose |
|---|---|
| `--s-overlay-bg` | Overlay bg |
| `--s-overlay-blur` | Overlay blur |
| `--s-border-style` | Border style |
| `--s-border` | Border |
| `--s-surface` | Surface |
| `--s-dialog-shadow` | Dialog shadow |
| `--s-text` | Text |
| `--s-card-radius` | Card radius |
| `--s-duration-fast` | Duration fast |
| `--s-radius-sm` | Radius sm |
| `--s-ring` | Ring |
| `--s-primary` | Primary |
