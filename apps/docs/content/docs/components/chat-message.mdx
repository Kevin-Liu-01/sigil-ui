---
title: ChatMessage
description: ChatMessage is a token-driven Sigil component in the developer and ai/product ui group.
---

## Import

```tsx
import { ChatMessage } from "@sigil-ui/components";
```

## Preview

<ComponentPreview>
  <ChatMessage role="assistant">Components stay token-first.</ChatMessage>
</ComponentPreview>

## Usage

```tsx
<ChatMessage role="assistant">Components stay token-first.</ChatMessage>
```

## CLI

```bash
npx @sigil-ui/cli add chat-message
```

## Token Integration

ChatMessage follows the Sigil component rules: styles resolve from `var(--s-*)`, class names are mergeable through `className`, and the component is exported from `@sigil-ui/components`.
