---
title: NotificationList
description: NotificationList is a token-driven Sigil component in the feedback and status group.
---

## Import

```tsx
import { NotificationList } from "@sigil-ui/components";
```

## Preview

<ComponentPreview>
  <NotificationList><Notification title="One" description="First notification" /><Notification title="Two" description="Second notification" /></NotificationList>
</ComponentPreview>

## Usage

```tsx
<NotificationList><Notification title="One" description="First notification" /><Notification title="Two" description="Second notification" /></NotificationList>
```

## CLI

```bash
npx @sigil-ui/cli add notification-list
```

## Token Integration

NotificationList follows the Sigil component rules: styles resolve from `var(--s-*)`, class names are mergeable through `className`, and the component is exported from `@sigil-ui/components`.
