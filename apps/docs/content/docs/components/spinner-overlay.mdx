---
title: SpinnerOverlay
description: SpinnerOverlay is a token-driven Sigil component in the feedback and status group.
---

## Import

```tsx
import { SpinnerOverlay } from "@sigil-ui/components";
```

## Preview

<ComponentPreview>
  <div className="relative h-32 rounded-[var(--s-card-radius)] border border-[var(--s-border)]"><SpinnerOverlay /></div>
</ComponentPreview>

## Usage

```tsx
<div className="relative h-32 rounded-[var(--s-card-radius)] border border-[var(--s-border)]"><SpinnerOverlay /></div>
```

## CLI

```bash
npx @sigil-ui/cli add spinner-overlay
```

## Token Integration

SpinnerOverlay follows the Sigil component rules: styles resolve from `var(--s-*)`, class names are mergeable through `className`, and the component is exported from `@sigil-ui/components`.
