---
title: DataFilters
description: DataFilters is a token-driven Sigil component in the data display and tables group.
---

## Import

```tsx
import { DataFilters } from "@sigil-ui/components";
```

## Preview

<ComponentPreview>
  <DataFilters searchPlaceholder="Filter rows..." />
</ComponentPreview>

## Usage

```tsx
<DataFilters searchPlaceholder="Filter rows..." />
```

## CLI

```bash
npx @sigil-ui/cli add data-filters
```

## Token Integration

DataFilters follows the Sigil component rules: styles resolve from `var(--s-*)`, class names are mergeable through `className`, and the component is exported from `@sigil-ui/components`.
