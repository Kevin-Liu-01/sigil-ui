---
title: TableOfContents
description: TableOfContents is a token-driven Sigil component in the layout, navigation, and content group.
---

## Import

```tsx
import { TableOfContents } from "@sigil-ui/components";
```

## Preview

<ComponentPreview>
  <TableOfContents items={[{ href: "#preview", label: "Preview" }, { href: "#usage", label: "Usage" }]} />
</ComponentPreview>

## Usage

```tsx
<TableOfContents items={[{ href: "#preview", label: "Preview" }, { href: "#usage", label: "Usage" }]} />
```

## CLI

```bash
npx @sigil-ui/cli add table-of-contents
```

## Token Integration

TableOfContents follows the Sigil component rules: styles resolve from `var(--s-*)`, class names are mergeable through `className`, and the component is exported from `@sigil-ui/components`.
