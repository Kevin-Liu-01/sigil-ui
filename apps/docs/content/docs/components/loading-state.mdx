---
title: LoadingState
description: LoadingState is a token-driven Sigil component in the feedback and status group.
---

## Import

```tsx
import { LoadingState } from "@sigil-ui/components";
```

## Preview

<ComponentPreview>
  <LoadingState title="Loading" description="Fetching component metadata." />
</ComponentPreview>

## Usage

```tsx
<LoadingState title="Loading" description="Fetching component metadata." />
```

## CLI

```bash
npx @sigil-ui/cli add loading-state
```

## Token Integration

LoadingState follows the Sigil component rules: styles resolve from `var(--s-*)`, class names are mergeable through `className`, and the component is exported from `@sigil-ui/components`.
