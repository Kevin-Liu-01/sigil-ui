---
title: Spotlight
description: Spotlight is a token-driven Sigil component in the overlay and interaction group.
---

## Import

```tsx
import { Spotlight } from "@sigil-ui/components";
```

## Preview

<ComponentPreview>
  <Modal trigger={<Button variant="outline">Open spotlight-style demo</Button>} title="Spotlight" description="Use Spotlight when the page owns an open state." />
</ComponentPreview>

## Usage

```tsx
<Modal trigger={<Button variant="outline">Open spotlight-style demo</Button>} title="Spotlight" description="Use Spotlight when the page owns an open state." />
```

## CLI

```bash
npx @sigil-ui/cli add spotlight
```

## Token Integration

Spotlight follows the Sigil component rules: styles resolve from `var(--s-*)`, class names are mergeable through `className`, and the component is exported from `@sigil-ui/components`.
