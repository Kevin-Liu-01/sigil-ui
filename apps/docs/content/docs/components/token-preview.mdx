---
title: TokenPreview
description: TokenPreview is a token-driven Sigil component in the developer and ai/product ui group.
---

## Import

```tsx
import { TokenPreview } from "@sigil-ui/components";
```

## Preview

<ComponentPreview>
  <TokenPreview name="--s-primary" />
</ComponentPreview>

## Usage

```tsx
<TokenPreview name="--s-primary" />
```

## CLI

```bash
npx @sigil-ui/cli add token-preview
```

## Token Integration

TokenPreview follows the Sigil component rules: styles resolve from `var(--s-*)`, class names are mergeable through `className`, and the component is exported from `@sigil-ui/components`.
