---
title: ImageUpload
description: ImageUpload is a token-driven Sigil component in the forms and inputs group.
---

## Import

```tsx
import { ImageUpload } from "@sigil-ui/components";
```

## Preview

<ComponentPreview>
  <ImageUpload label="Upload image" />
</ComponentPreview>

## Usage

```tsx
<ImageUpload label="Upload image" />
```

## CLI

```bash
npx @sigil-ui/cli add image-upload
```

## Token Integration

ImageUpload follows the Sigil component rules: styles resolve from `var(--s-*)`, class names are mergeable through `className`, and the component is exported from `@sigil-ui/components`.
