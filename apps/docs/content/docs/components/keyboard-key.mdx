---
title: KeyboardKey
description: KeyboardKey is a token-driven Sigil component in the developer and ai/product ui group.
---

## Import

```tsx
import { KeyboardKey } from "@sigil-ui/components";
```

## Preview

<ComponentPreview>
  <KeyboardKey>⌘K</KeyboardKey>
</ComponentPreview>

## Usage

```tsx
<KeyboardKey>⌘K</KeyboardKey>
```

## CLI

```bash
npx @sigil-ui/cli add keyboard-key
```

## Token Integration

KeyboardKey follows the Sigil component rules: styles resolve from `var(--s-*)`, class names are mergeable through `className`, and the component is exported from `@sigil-ui/components`.
