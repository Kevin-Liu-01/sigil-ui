---
title: VersionBadge
description: VersionBadge is a token-driven Sigil component in the developer and ai/product ui group.
---

## Import

```tsx
import { VersionBadge } from "@sigil-ui/components";
```

## Preview

<ComponentPreview>
  <VersionBadge version="0.1.0" />
</ComponentPreview>

## Usage

```tsx
<VersionBadge version="0.1.0" />
```

## CLI

```bash
npx @sigil-ui/cli add version-badge
```

## Token Integration

VersionBadge follows the Sigil component rules: styles resolve from `var(--s-*)`, class names are mergeable through `className`, and the component is exported from `@sigil-ui/components`.
