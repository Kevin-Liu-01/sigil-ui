---
title: InlineAlert
description: InlineAlert is a token-driven Sigil component in the feedback and status group.
---

## Import

```tsx
import { InlineAlert } from "@sigil-ui/components";
```

## Preview

<ComponentPreview>
  <InlineAlert variant="info">Inline alert content</InlineAlert>
</ComponentPreview>

## Usage

```tsx
<InlineAlert variant="info">Inline alert content</InlineAlert>
```

## CLI

```bash
npx @sigil-ui/cli add inline-alert
```

## Token Integration

InlineAlert follows the Sigil component rules: styles resolve from `var(--s-*)`, class names are mergeable through `className`, and the component is exported from `@sigil-ui/components`.
