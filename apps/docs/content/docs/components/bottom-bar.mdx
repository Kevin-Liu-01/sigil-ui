---
title: BottomBar
description: BottomBar is a token-driven Sigil component in the layout, navigation, and content group.
---

## Import

```tsx
import { BottomBar } from "@sigil-ui/components";
```

## Preview

<ComponentPreview>
  <BottomBar>Bottom bar content</BottomBar>
</ComponentPreview>

## Usage

```tsx
<BottomBar>Bottom bar content</BottomBar>
```

## CLI

```bash
npx @sigil-ui/cli add bottom-bar
```

## Token Integration

BottomBar follows the Sigil component rules: styles resolve from `var(--s-*)`, class names are mergeable through `className`, and the component is exported from `@sigil-ui/components`.
