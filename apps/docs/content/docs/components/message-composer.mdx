---
title: MessageComposer
description: MessageComposer is a token-driven Sigil component in the developer and ai/product ui group.
---

## Import

```tsx
import { MessageComposer } from "@sigil-ui/components";
```

## Preview

<ComponentPreview>
  <MessageComposer placeholder="Message..." />
</ComponentPreview>

## Usage

```tsx
<MessageComposer placeholder="Message..." />
```

## CLI

```bash
npx @sigil-ui/cli add message-composer
```

## Token Integration

MessageComposer follows the Sigil component rules: styles resolve from `var(--s-*)`, class names are mergeable through `className`, and the component is exported from `@sigil-ui/components`.
