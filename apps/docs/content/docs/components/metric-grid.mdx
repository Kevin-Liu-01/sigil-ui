---
title: MetricGrid
description: MetricGrid is a token-driven Sigil component in the data display and tables group.
---

## Import

```tsx
import { MetricGrid } from "@sigil-ui/components";
```

## Preview

<ComponentPreview>
  <MetricGrid><StatCard label="Components" value="300+" /><StatCard label="Presets" value="44" /></MetricGrid>
</ComponentPreview>

## Usage

```tsx
<MetricGrid><StatCard label="Components" value="300+" /><StatCard label="Presets" value="44" /></MetricGrid>
```

## CLI

```bash
npx @sigil-ui/cli add metric-grid
```

## Token Integration

MetricGrid follows the Sigil component rules: styles resolve from `var(--s-*)`, class names are mergeable through `className`, and the component is exported from `@sigil-ui/components`.
