---
title: ActivityTimeline
description: ActivityTimeline is a token-driven Sigil component in the developer and ai/product ui group.
---

## Import

```tsx
import { ActivityTimeline } from "@sigil-ui/components";
```

## Preview

<ComponentPreview>
  <ActivityTimeline items={[{ title: "Component added", description: "Modal docs generated." }, { title: "Build passed", description: "Docs compiled." }]} />
</ComponentPreview>

## Usage

```tsx
<ActivityTimeline items={[{ title: "Component added", description: "Modal docs generated." }, { title: "Build passed", description: "Docs compiled." }]} />
```

## CLI

```bash
npx @sigil-ui/cli add activity-timeline
```

## Token Integration

ActivityTimeline follows the Sigil component rules: styles resolve from `var(--s-*)`, class names are mergeable through `className`, and the component is exported from `@sigil-ui/components`.
