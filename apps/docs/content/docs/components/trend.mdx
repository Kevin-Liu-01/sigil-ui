---
title: Trend
description: Trend is a token-driven Sigil component in the data display and tables group.
---

## Import

```tsx
import { Trend } from "@sigil-ui/components";
```

## Preview

<ComponentPreview>
  <Trend value={24} />
</ComponentPreview>

## Usage

```tsx
<Trend value={24} />
```

## CLI

```bash
npx @sigil-ui/cli add trend
```

## Token Integration

Trend follows the Sigil component rules: styles resolve from `var(--s-*)`, class names are mergeable through `className`, and the component is exported from `@sigil-ui/components`.
