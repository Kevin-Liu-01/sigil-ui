---
title: Notification
description: Notification is a token-driven Sigil component in the feedback and status group.
---

## Import

```tsx
import { Notification } from "@sigil-ui/components";
```

## Preview

<ComponentPreview>
  <Notification title="Deploy complete" description="Your component docs were published." unread />
</ComponentPreview>

## Usage

```tsx
<Notification title="Deploy complete" description="Your component docs were published." unread />
```

## CLI

```bash
npx @sigil-ui/cli add notification
```

## Token Integration

Notification follows the Sigil component rules: styles resolve from `var(--s-*)`, class names are mergeable through `className`, and the component is exported from `@sigil-ui/components`.
